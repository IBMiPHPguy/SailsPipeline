[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: API tests that require a database (MySQL in Docker, SQLite locally)
